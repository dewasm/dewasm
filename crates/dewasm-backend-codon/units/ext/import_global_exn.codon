# requires: ext/extern, rt/boxed, rt/link_error
@staticmethod
def import_global_exn(e: Rt.Extern, mod: str, name: str) -> Rt.Global[Optional[Rt.WasmException]]:
    g = e.g_exn
    if g is not None:
        return g
    raise Rt.LinkError("incompatible import type for " + mod + "." + name)
