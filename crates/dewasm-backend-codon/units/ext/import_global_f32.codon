# requires: ext/extern, rt/link_error
@staticmethod
def import_global_f32(e: Rt.Extern, mod: str, name: str) -> Rt.Global[float32]:
    g = e.g_f32
    if g is not None:
        return g
    raise Rt.LinkError("incompatible import type for " + mod + "." + name)
