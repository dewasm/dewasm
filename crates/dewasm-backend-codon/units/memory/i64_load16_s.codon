# requires: rt/trap, rt/raw_load16
def i64_load16_s(self, a: int) -> UInt[64]:
    if a + 2 > self.n:
        Rt.trap("out of bounds memory access")
    return UInt[64](Int[64](Int[16](Rt.raw_load16(self.data + a))))
