# requires: rt/trap, rt/raw_load32
def i64_load32_s(self, a: int) -> UInt[64]:
    if a + 4 > self.n:
        Rt.trap("out of bounds memory access")
    return UInt[64](Int[64](Int[32](Rt.raw_load32(self.data + a))))
