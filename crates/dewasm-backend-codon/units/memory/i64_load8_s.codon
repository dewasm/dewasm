# requires: rt/trap, rt/raw_load8
def i64_load8_s(self, a: int) -> UInt[64]:
    if a + 1 > self.n:
        Rt.trap("out of bounds memory access")
    return UInt[64](Int[64](Int[8](Rt.raw_load8(self.data + a))))
