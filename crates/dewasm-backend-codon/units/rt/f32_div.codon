# IEEE division without the host language's zero-divisor exception (wasm float division never traps).
@llvm
@staticmethod
def f32_div(a: float32, b: float32) -> float32:
    %r = fdiv float %a, %b
    ret float %r
