# requires: rt/trap, rt/f64_trunc
@staticmethod
def i64_trunc_u(x: float) -> UInt[64]:
    if x != x:
        Rt.trap("invalid conversion to integer")
    t = Rt.f64_trunc(x)
    if t < 0.0 or t >= 18446744073709551616.0:
        Rt.trap("integer overflow")
    return UInt[64](t)
