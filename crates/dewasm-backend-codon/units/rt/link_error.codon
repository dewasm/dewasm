# A failed import resolution at instantiation time (missing import, or one of the wrong kind), kept distinct from Trap and from plain errors.
class LinkError(Exception):
    def __init__(self, msg: str):
        super().__init__(msg)
